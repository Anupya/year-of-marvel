{
  "name": "year-of-marvel",
  "description": "An app that lists all the Marvel comics published on a certain year",
  "version": "0.0.1",
  "devDependencies": {
    "mocha": "5.0.0",
    "supertest": "3.0.0",
    "unit.js": "^2.1.0"
  },
  "scripts": {
    "start": "node app.js",
    "test": "mocha --recursive tests --exit"
  }
}
